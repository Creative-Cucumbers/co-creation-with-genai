# KUAの雰囲気を取り入れた授業スライド

## 目的と参照

白い背景、黒い太字の見出し、ライトグレーの面、深い青のアクセントで、落ち着いた授業スライドを作る。余白と情報のまとまりで読み順を伝え、教室での投影でも読みやすくする。

- 視覚的な参考：[京都芸術大学・プロダクトデザインコース](https://www.kyoto-art.ac.jp/department/product/)
- 実装例：[2026/1_ai_basic/index.html](2026/1_ai_basic/index.html)
- 内容の対応例：[2026/1_ai_basic/readme.md](2026/1_ai_basic/readme.md)

以下の色・寸法は、このリポジトリの授業資料用に定めた値。大学の公式ブランド規定ではない。ロゴや大学サイトのナビゲーションを再現する必要はない。

## 画面と余白

- 基準キャンバスは **1600 × 900 px、16:9**。
- 画面全体に入る倍率 `Math.min(window.innerWidth / 1600, window.innerHeight / 900)` でキャンバスを均等に拡大・縮小する。リサイズ時も再計算する。
- キャンバスを画面中央に置き、4:3や縦長画面では外側に余白を残す。縦横を別々に伸縮しない。
- スライドの内側余白は上70px、左右80px、下100px。下部には講義名・操作ボタンの領域を確保する。
- ヘッダーの下に2pxの黒い罫線。ヘッダー下側の内側余白24px、本文との間隔28px。
- 本文は基本2列、列間24px。比較は2列、3つの理由・手順は3列、表は全幅を使う。
- 本文をスライド内でスクロールさせない。`overflow: hidden` で隠れているだけの状態を「収まっている」と判断しない。

## 配色

| 用途 | 値 | 使い方 |
| --- | --- | --- |
| 本文・見出し | `#242424` | 主役となる文字、区切り線 |
| 補助文字 | `#666666` | 副題、キャプション |
| アクセント | `#004898` | リンク、年号、番号、強調線 |
| スライド背景 | `#ffffff` | 基本の白地 |
| 通常カード | `#f2f3f4` | 比較や説明のまとまり |
| 強調カード | `#edf3f9` | 定義、重要なメッセージ、指示例 |
| 細い境界線 | `#d9dde2` | カード・表・ボタンの境界 |
| キャンバス外側 | `#e6e8eb` | 余白の背景 |

CSSカスタムプロパティを使い、同じ用途の色を統一する。装飾のために色数を増やさない。図の分類に必要な色は使えるが、ラベルや形でも意味が伝わるようにする。

## 文字組み

フォントは実装例と同じスタックを基本にする。

```css
font-family: "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN",
  "Yu Gothic", Meiryo, sans-serif;
```

以下は1600 × 900 px上での基準値。

| 要素 | サイズ | 補足 |
| --- | --- | --- |
| 通常のスライド見出し | 44px | 太字、行高1.4 |
| 表紙タイトル | 112px | 短い科目名の場合。長い名称は調整する |
| 副題 | 20px | グレー |
| カード見出し | 26px | 太字、下余白16px |
| 本文 | 21〜23px | 行高1.7、標準23px |
| 表の文字 | 21px | 長文を詰め込まない |
| 出典・キャプション | 16〜18px | 補助情報に限定する |
| 年度・ページ番号・フッター | 15〜16px | 主本文と競合させない |

本文を収めるためだけに文字を小さくしない。まず重複を削り、文を短くし、配置を変える。見出しへの装飾的な絵文字は使わず、太さ・番号・余白で階層を示す。

## カードと情報のまとまり

- カードは原則四角形。背景はライトグレー、上辺に3pxの細い線、内側余白は28px × 30px。
- 強調カードには薄い青の背景と青い上辺を使う。
- 大きな角丸、グラデーション、カードごとの強い影は使わない。キャンバス外側の薄い影は可。
- **見出しとその本文・リストは同じコンテナに入れる。** Gridの自動配置で見出しだけ別の列へ移動しないようにする。
- 一つのページで伝える主題を決める。内容が少ないページは余白を残し、説明を水増ししない。

## よく使うページ構成

### 2つの事例を比較する

左右に同じ幅のカードを並べ、それぞれに時期、組織名、画像、要点、出典を入れる。下段に共通する学びを全幅の強調カードで置く。

DeNA・みずほのように比較して見せたい事例は、画像を小さくしても**1ページにまとめる**。画像だけの別ページは自動で追加しない。参考寸法は画像列180px、説明との間隔20px、画像の表示領域の高さ220px。

ニュースの公開日と取り組みの実施時期は区別して書く。「7月4日」だけでなく「2025年7月4日のニュース」など、年度をまたいでも意味が明確な表記にする。

### 手を動かす実習

タイトルは「AIとつくる、はじめてのゲーム」のように、体験する内容が伝わる言葉にする。

1. 上段：ログイン・作成・試して改良など、短い3ステップ。
2. 中段：そのまま入力できる指示例を全幅の強調カードに掲載。
3. 下段：主実習以外のサービスは「今回は紹介のみ」として小さくまとめる。

説明を盛り込みすぎず、1ページを見ながら実習を進められるようにする。

### 質問・図・表

- 質問だけの演習は、短い質問を大きく並べる。不要なログイン説明や長い例文を足さない。
- 年表は年代順に並べ、矢印・番号で順序を示す。複数段なら次に読む位置を明記する。
- フロー図は工程を箱で囲み、矢印でつなぐ。READMEのMermaid図は、同じ意味のHTML/CSSやSVGの図として実装してよい。
- 表は見出し行を青地に白文字、セルは細い境界線で区切る。表と図を比較する場合は横に並べる。

## 画像・出典

- READMEにある画像・図は、対応するスライドにも掲載する。ローカル画像は原則、授業ディレクトリの `images/` を相対パスで参照する。
- 画像は縦横比を保ち、図・記事画像は `object-fit: contain` で全体を表示する。文字や凡例を切り落とさない。
- 小さな記事画像はクリックで原寸画像を開けるようにする。要点は画像の文字を読むことに頼らず、本文にも書く。
- 内容がわかる `alt` を付け、必要に応じてキャプションと出典リンクを添える。
- リンクは青色と下線で示す。別タブで開くリンクには `rel="noopener noreferrer"` を付ける。

## 操作

- 年度・科目名は左上、現在ページ／総ページ数は右上、講義名は左下、前へ・次へは右下に配置する。
- 操作部品を本文・画像に重ねない。総ページ数はスライド数から算出し、追加・削除時に初期表示も合わせる。
- 左右キーで移動できるようにする。リンク・ボタン・入力欄の操作中は、スライド移動のキー処理を割り込ませない。
- キーボードフォーカスを見えるようにする。下端の進捗バーや控えめな動きは可。動きを減らす設定に対応する。

## 変更後の確認

1. 変更したページをブラウザで開き、スクリーンショットで文字・余白・画像の配置を確認する。
2. 本文領域の縦横のはみ出しと、子要素が本文・操作領域の境界を越えていないことを確認する。画像が読み込まれていることも確かめる。
3. 共通CSSや拡大・縮小処理を変更した場合は、全ページを巡回し、16:9画面と4:3画面で確認する。
4. ページ追加・削除や操作処理を変更した場合は、ページ数、前後移動、最初・最後のボタン状態を確認する。
5. READMEとの内容・画像・図の対応を確認し、`git diff --check` を実行する。

収まらない場合は、文章の整理、重複の削除、列構成・画像サイズの調整の順に検討する。ユーザーが1ページと指定している場合は、その制約を維持する。
